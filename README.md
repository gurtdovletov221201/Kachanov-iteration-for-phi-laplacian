# Kachanov iteration for Orlicz Type Energy Functionals

We are looking for the energy minimization where 

$$
\inf_{u \in W^{1, \phi}_{0}(\Omega)} E(u) := \int_{\Omega} \phi(u) dx - F(u)
$$

where $\phi$ is the N-function. 

